id: sharepoint
name: Microsoft SharePoint
provider: microsoft
icon: 📁
category: storage

auth:
  type: oauth2
  provider: microsoft
  scope: graph
  requiredScopes:
    - Files.ReadWrite.All
    - Sites.Read.All

actions:
  - id: sharepoint.list
    name: List Files
    description: Listet Dateien in einem SharePoint-Ordner auf
    method: GET
    urlTemplate: "https://graph.microsoft.com/v1.0/drives/{{driveId}}/{{folderRef}}/children"
    input:
      type: object
      properties:
        driveId:
          type: string
          required: true
          description: SharePoint Drive ID
        folderRef:
          type: string
          default: root
          description: "Ordner-Referenz: 'root' für Stammverzeichnis oder 'root:/Pfad/Zum/Ordner:' für Unterordner"

  - id: sharepoint.read
    name: Read File
    description: Liest den Inhalt einer Datei aus SharePoint
    method: GET
    urlTemplate: "https://graph.microsoft.com/v1.0/drives/{{driveId}}/root:{{path}}:/content"
    responseType: binary
    input:
      type: object
      properties:
        driveId:
          type: string
          required: true
        path:
          type: string
          required: true

  - id: sharepoint.upload
    name: Upload File
    description: Laedt eine Datei in SharePoint hoch
    method: PUT
    urlTemplate: "https://graph.microsoft.com/v1.0/drives/{{driveId}}/root:{{path}}:/content"
    input:
      type: object
      properties:
        driveId:
          type: string
          required: true
        path:
          type: string
          required: true
        content:
          type: string
          required: true
